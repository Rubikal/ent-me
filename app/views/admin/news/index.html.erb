<ul class="nav nav-pills sub-nav">
  <li>
    <nav class="vendor-breadcrumb">
      <ol class="cd-breadcrumb narrow triangle custom-icons">
        <li class="home"><a href="<%= admin_news_index_path %>"><i class="fa fa-cubes" aria-hidden="true">&nbsp;</i> News</a></li>
      </ol>
    </nav>
  </li>
</ul>

<div>
  <div class="grid-container container">
    <div class="grid-card">
      <div class="actions">
        <ul class="tab-list">
          <li role="presentation">
            <a class="tab active" href="/admin/news">All News</a>
          </li>
        </ul>
      </div>
      <div class="grid-content">
        <% if @news.count == 0 %>
          <div class="row">
            <div class="col-md-offset-4 col-md-4 empty-placeholder">
              There are no news in shop.
            </div>
          </div>
        <% else %>
          <table class="table table-hover">
            <thead>
              <tr>
                <th></th>
                <th class="avatar-head"></th>
                <th>Title</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% @news.each do |news| %>
                <tr>
                  <td></td>
                  <td scope="row">
                    <%= link_to admin_news_path(news), class: 'avatar' do %>
                      <!-- Product thumb -->
                      <%= image_tag(news.image.thumb.url, size: "60x60", alt: "Avatar") %>
                    <% end %>
                  </td>
                  <td>
                    <%= link_to admin_news_path(news) do %>
                      <%= news.title %>
                    <% end %>
                  </td>
                  <td>
                    <%= link_to admin_news_path(news), method: :delete, class: 'danger', data: {confirm: 'Product will be permanently deleted from Shopify store. Are you sure you want to delete?'} do %>
                      <i class="fa fa-trash"></i>
                      Remove
                    <% end %>
                  </td>
                  <td></td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%= paginate @news %>
        <% end %>
      </div>
    </div>
  </div>
</div>
